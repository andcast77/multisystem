# API Fastify (misma base que usa el Hub). Cliente principal: src/lib/api/client.ts
# En Vite, definir también en build si usas solo prefijos VITE_ — ver apps/shopflow/README.md
NEXT_PUBLIC_API_URL=http://localhost:3000

# Auth auxiliar (src/lib/api-client.ts)
# VITE_API_URL=http://localhost:3000

# Push notifications (opcional)
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=

NODE_ENV=development
