# Hub (Next.js)
# Copy this file to apps/hub/.env and adjust values per environment.

# Cloudflare Turnstile site key (PUBLIC). Debe emparejarse con TURNSTILE_SECRET_KEY del API (mismo widget).
# Prueba: 1x00000000000000000000AA — solo válida con secret de prueba 1x0000000000000000000000000000000AA en el API.
# Producción: site key del widget en Cloudflare (hostname del Hub registrado); si falta, el build usa la clave de prueba
# y el API con secret real devolverá CAPTCHA_FAILED / invalid-input-response.
# https://developers.cloudflare.com/turnstile/troubleshooting/testing/
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Central API base URL. Leave empty in local dev to use same-origin `/v1` via Next rewrites (no CORS).
# Set to an absolute URL (e.g. http://localhost:3000) only if you need direct cross-origin calls.
NEXT_PUBLIC_API_URL=

# Public URLs for the ecosystem (landing "Módulos integrados" cards and dashboard module links).
# Required in production/preview: Next.js bakes NEXT_PUBLIC_* at build time — configure them in
# Vercel (or your host) for Production and Preview before deploying, or links fall back to localhost.
NEXT_PUBLIC_HUB_URL=<HUB_APP_URL>
NEXT_PUBLIC_SHOPFLOW_URL=<SHOPFLOW_APP_URL>
NEXT_PUBLIC_WORKIFY_URL=<WORKIFY_APP_URL>
NEXT_PUBLIC_TECHSERVICES_URL=<TECHSERVICES_APP_URL>
