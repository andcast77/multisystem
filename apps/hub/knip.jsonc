{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  // Keep Knip focused on source files only (avoid dist/.next noise).
  "project": [
    "src/**/*",
    "!src/**/__tests__/**",
    "!src/**/*.test.ts",
    "!src/**/*.test.tsx",
    "!src/**/*.spec.ts",
    "!src/**/*.spec.tsx"
  ]
}
