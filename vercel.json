{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -w run build:api",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api/$1" }],
  "functions": {
    "api/index.ts": {
      "includeFiles": "packages/api/dist/**"
    }
  }
}
